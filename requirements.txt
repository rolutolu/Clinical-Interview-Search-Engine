# ═══════════════════════════════════════════
# Clinical Interview IR System — Streamlit Cloud Dependencies
# ═══════════════════════════════════════════
# This file is used by Streamlit Cloud for deployment.
# It includes ONLY the lightweight packages needed for the web UI.
#
# For full local/Colab development (with pyannote, torch, etc.):
#   pip install -r requirements-full.txt
# ═══════════════════════════════════════════

# ── Core ──
streamlit>=1.28.0
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.24.0

# ── Database ──
supabase>=2.0.0

# ── API Calls (Groq Whisper + LLM) ──
httpx>=0.25.0
groq>=0.4.0

# ── Evaluation & Visualization ──
scikit-learn>=1.3.0
matplotlib>=3.7.0
plotly>=5.15.0

# ── Audio Processing (chunking large files) ──
pydub>=0.25.1
